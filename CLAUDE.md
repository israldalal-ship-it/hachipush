# החיפוש – דף נחיתה לספר

דף נחיתה (landing page) למכירת הספר "החיפוש" של הרב אדם סיני. אתר סטטי, בעברית, RTL, בלי build process או backend אמיתי — שני קבצי HTML עצמאיים שמכילים הכל inline (CSS + JS בתוך הקובץ).

## מבנה הפרויקט

- `index.html` — גרסת דסקטופ של דף הנחיתה. בראש ה-`<head>` יש סקריפט שמזהה מכשיר מובייל (לפי `userAgent` ולפי רוחב מסך) ומפנה אוטומטית ל-`mobile.html`.
- `mobile.html` — גרסת מובייל (כפילות עצמאית, לא responsive משותף — שינוי בעיצוב/תוכן צריך להתבצע בשני הקבצים בנפרד). שמה היה בעבר `index mobile.html` (עם רווח) — שונה כדי שלא ייצרו תקלות קידוד URL. גם בקובץ הזה יש בראש ה-`<head>` סקריפט הפוך שמזהה מכשיר דסקטופ ומפנה בחזרה ל-`index.html`.
- `FIREBASE_SETUP.md` — מדריך הקמת Firebase (כבר מוגדר בקוד, פרויקט `hahipus`).
- `אפיון.txt` / `אפיון לAI.txt` — מסמכי האפיון/אסטרטגיה המקוריים של האתר (חזון, קהל מטרה, מבנה אזורים, טון).
- תמונות/וידאו בשורש (`art1-4`, `book_cover.jpg`, `logo*.png`, `rabbi_photo.jpg`, `video1.mp4`, `0603 (1).mp4`, וכו') — נכסי מדיה המשמשים את שני קבצי ה-HTML.
- `hahipus/` — תיקייה ריקה כרגע.
- `1` — קובץ תמונה (PNG) בלי סיומת.

אין `package.json`, אין build tool — עריכה היא עריכה ישירה של ה-HTML.

## עיצוב ומיתוג

- גופן: Heebo (Google Fonts), RTL מלא (`dir="rtl"`).
- צבעים: `--blue:#0066ff`, `--cream:#fbf9f5`, `--ink:#0F172A`, `--ink-soft:#1E293B`, `--ink-faded:#64748B`, `--gold:#F59E0B`.
- סגנון: מודרני, נקי, רגשי, קולנועי. Mobile-first (רוב התנועה ממובייל).

## מבנה הדף (לפי האפיון)

Hero → הסיפור → שאלות החיים → מה הקורא יקבל → קטעים מהספר → המלצות קוראים → מאחורי הספר (הרב אדם סיני) → פרק ראשון במתנה (ליד) → רכישה → המשך המסע.

מסר מרכזי: האתר לא מוכר ספר — הוא מוכר הזדהות, משמעות וחיפוש אמת. להציג כ"רומן מרתק", לא כ"ספר קבלה" וגם לא כספר דתי/הטפה.

## אינטגרציות

- **Firebase** (Auth + Firestore, פרויקט `hahipus`): התחברות עם Google וכתיבת המלצות קוראים (`endorsements` collection). המלצות חדשות נכנסות עם `approved:false` ודורשות אישור ידני בקונסולת Firestore לפני שמוצגות באתר. ה-config כבר ממולא בקוד (לא placeholder).
- **Zapier webhook** (`https://hooks.zapier.com/hooks/catch/...`): טופס הליד ("פרק ראשון במתנה") שולח name/email/phone ל-Zapier ב-POST. אין שמירה מקומית של לידים — Zapier הוא המקור היחיד לאיסוף.

## נקודות לתשומת לב

- שינוי בתוכן/CTA/עיצוב חייב להתבצע גם ב-`index.html` וגם ב-`mobile.html` — אין שיתוף קוד בין הקבצים.
- אם משנים קונפיגורציית Firebase, יש לעדכן בשני הקבצים.
- בכל קובץ קיימות שתי הגדרות של הפונקציה ששולחת את הטופס (`submitLead`) — אחת ב-script רגיל, אחת נוספת שמוגדרת מאוחר יותר בתוך ה-script מסוג `module` (זו שב-`module` היא זו שבאמת רצה, כי היא נטענת אחרונה ודורסת את הקודמת). שינוי בלוגיקת שליחת הליד חייב להתבצע בשתיהן, בשני הקבצים.
- מספר טלפון מהטופס מנורמל בקוד (פונקציית `formatPhoneIL`) לפורמט בינלאומי (`+972...`) לפני שליחה ל-Zapier, כדי שלא יידחה ע"י כלי הדיוור (Brevo) שדורש פורמט E.164.
- קובץ `1` הוא תמונה בפועל (PNG) — כדאי לשנות שם לסיומת ברורה אם משתמשים בו.
- ריפו git קיים, מחובר ל-remote ב-GitHub: `https://github.com/israldalal-ship-it/hachipush.git`, branch `main`. ייתכן שהאתר החי נמשך ישירות מהריפו הזה (למשל GitHub Pages או שירות אחר שמתחבר ל-GitHub) — לכן יש לדחוף (`push`) שינויים כדי שיופיעו באתר החי.
